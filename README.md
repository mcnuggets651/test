# Apex Price Risk

Standalone, non-serving FPL price-change forecasting and transfer-route risk system.

**Safety status:** isolated from `mcnuggets651/fpl-apex`. This repository must never become a required dependency of Apex V2 production. Its outputs are advisory-only until prospective calibration and an explicit governance promotion.

See `docs/ARCHITECTURE.md` and `docs/GOVERNANCE.md` for the hard boundary.